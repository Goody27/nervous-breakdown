# 移動
```
cd nervous-breakdown
```

# インストールできているか確認
```
docker compose run --rm app mix --version
```
# deps.get
```
docker compose run --rm app mix deps.get
```
 # スタート
 ```
 docker compose up -d
 ```
 # コンテナ削除
 ```
  docker compose down
 ```
